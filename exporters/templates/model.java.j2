package {{ package }}.models;

import jakarta.persistence.*;
import java.time.*;
import java.util.*;

@Entity
public class {{ class_name }} {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    {% for attr in attributes if attr.name|lower != "id" %}
    private {{ attr.type }} {{ attr.name }};
    {% endfor %}

    // ===== Getters & Setters =====
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }

    {% for attr in attributes if attr.name|lower != "id" %}
    public {{ attr.type }} get{{ attr.name|capitalize }}() { return {{ attr.name }}; }
    public void set{{ attr.name|capitalize }}({{ attr.type }} {{ attr.name }}) { this.{{ attr.name }} = {{ attr.name }}; }
    {% endfor %}
}
