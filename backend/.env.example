# Base de datos
DB_URL=postgresql://postgres:changeme@localhost:5432/uml_collab_tool

# JWT
JWT_SECRET=changeme-generate-a-real-secret
JWT_ALG=HS256
ACCESS_EXPIRE_MIN=480
REFRESH_EXPIRE_DAYS=14

# CORS
CORS_ORIGINS=http://localhost:5173

# Debug mode - NUNCA true en producción/demo: deshabilita la validación de token en get_current_user
DEBUG=false

# Asistente de IA (edición del diagrama por texto/voz)
ANTHROPIC_API_KEY=changeme
